const num: number = 42;
const message: string = "Hello, TypeScript!";
const isTrue: boolean = true;


// We create an array of numbers using the 'number[]' syntax.
const numbers: number[] = [1, 2, 3, 4, 5];

// We create an array of strings using the 'string[]' syntax.
const strings: string[] = ["Hello", "World"];


// We create a tuple, which is a fixed-length array with elements of different types.
const tupleExample: [string, number] = ["Age", 30];